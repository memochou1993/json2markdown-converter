const RenderMode = Object.freeze({
  HTML: 'html',
  MARKDOWN: 'markdown',
  PREVIEW: 'preview',
});

const ViewMode = Object.freeze({
  EDIT: 'edit',
  SPLIT: 'split',
  VIEW: 'view',
});

export {
  RenderMode,
  ViewMode,
};
